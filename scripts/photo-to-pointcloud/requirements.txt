# Pinned для воспроизводимости — Depth-Anything через transformers требует
# свежий torch и transformers (depth-anything-v2 завезли в 4.45+).
# Для M-series Mac torch ставится с поддержкой MPS из коробки.

numpy>=1.26,<3.0
Pillow>=10.0
torch>=2.4
transformers>=4.45
rembg>=2.0.50
onnxruntime>=1.18
